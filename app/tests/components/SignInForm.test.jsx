var React = require('react');
var ReactDOM = require('react-dom');
var expect = require('expect');
var TestUtils = require('react-addons-test-utils');

var SignInForm = require('SignInForm');

describe('SignInForm', () => {
    it('should exist', () => {
        expect(SignInForm).toExist();
    });
});